p #{@event.title} 的管理员, 您好：

p 19屋已经向 #{@user.login} 退款 #{@refund.amount} 元。

p 退款原因：#{@refund.reason}

p 订单号：#{@order.number}

p 订单包含以下门票：

- @order.items.each do |item|
  div #{item.quantity} x #{item.name}  单价：#{item.unit_price} 元
p 订单总额 ：#{@order.price} 元
