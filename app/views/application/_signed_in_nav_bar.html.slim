ul.nav
  - if can? :invite, User
    li = link_to t('labels.invitations'), invitations_path
  - if can? :refund, EventOrder
    li = link_to t('activerecord.models.event_order_refund'), refunds_path
  li = link_to t('labels.my_orders'), user_orders_path
